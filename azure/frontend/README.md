# Azure AI Stories Frontend

This folder contains a simple Next.js frontend scaffold for the Azure AI Stories app.

Run locally:

```
cd azure/frontend
npm install
npm run dev
```

The frontend calls the Azure Function story API at `API_BASE_URL`.
